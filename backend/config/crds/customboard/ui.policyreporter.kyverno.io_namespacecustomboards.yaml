---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v2.4.5-0.20250927090338-8022a0e22018+incompatible.dirty
  name: namespacecustomboards.ui.policyreporter.kyverno.io
spec:
  group: ui.policyreporter.kyverno.io
  names:
    kind: NamespaceCustomBoard
    listKind: NamespaceCustomBoardList
    plural: namespacecustomboards
    shortNames:
    - ncb
    singular: namespacecustomboard
  scope: Namespace
  versions:
  - additionalPrinterColumns:
    - jsonPath: .metadata.name
      name: Name
      priority: 1
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: NamespaceCustomBoard is the Schema for the namespace customboards
          API
        properties:
          accessControl:
            description: AccessControl is an optional reference to the custom boards
              access control
            oneOf:
            - required:
              - emails
            - required:
              - groups
            properties:
              emails:
                description: Source is an identifier for the policy engine that manages
                  this report
                items:
                  type: string
                type: array
              groups:
                description: Rule is the name or identifier of the rule within the
                  policy
                items:
                  type: string
                type: array
            type: object
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          display:
            description: Allowed values are "resources" or "results".
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          policyReports:
            description: PolicyReportSelector allows to select visualized reports
            properties:
              labelSelector:
                additionalProperties:
                  type: string
                type: object
            type: object
          sources:
            description: SourceSelector allows to select visualized sources
            properties:
              list:
                items:
                  type: string
                type: array
            type: object
        type: object
    served: true
    storage: true
    subresources: {}
